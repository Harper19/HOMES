process {
    // This is a very clear techniuqe to pass sample specific optional value arguments.
    // I do not think this violates any HOMES guidelines
    ext.args   = { " --model ${meta.model} " }
}
