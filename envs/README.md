# Environments

Use this directory for optional environment descriptions, such as:

```text
environment.yml
Dockerfile
```

For nf-core pipelines, prefer Docker/Singularity profiles over manually maintained conda environments.

